apiVersion: v1
kind: Pod
metadata:
  name: nginx-noncompliant
  namespace: default
spec:
  containers:
    - name: nginx
      image: nginx:latest
      resources:
        limits:
          memory: "256Mi"
          cpu: "500m"