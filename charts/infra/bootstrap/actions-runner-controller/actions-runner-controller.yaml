authSecret:
  # enabled: false
  create: false
  # name: "controller-manager"
  # annotations: {}

env:
  - name: GITHUB_TOKEN
    valueFrom:
      secretKeyRef:
        key: github_token
        name: controller-manager
        optional: false